# Node.js V8 Engine

A lightweight plugin for PEGASUS that enables direct execution of JavaScript (.js) files using the system's Node.js engine.

## Features

* Real-time console output mapping
* Native error and exception handling
* Asynchronous execution to prevent interface freezing
* Built-in dark mode terminal interface

## Entry Point

def execute(filepath, area):
